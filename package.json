{
  "name": "nodeapp",
  "version": "2.0.1",
  "description": "Basic webserver",
  "author": "frankc60",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "git": "git add . && echo 'do a git publish after comment.' && git commit -m",
    "publish": "git push",
    "deploy": "node -e \"const dateTime = require('moment')().format('YYYYMMDD')\" && `git add .` && git commit -m \"v${{dateTime}}\"" 
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/frankc60/hello-world"
  },
  "license": "ISC",
  "dependencies": {
    "express": "4.17.1",
    "moment": "2.29.1"
  }
}
